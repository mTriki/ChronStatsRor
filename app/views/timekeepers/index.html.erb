<h1>Listing timekeepers</h1>

<table>
  <thead>
    <tr>
      <th>Login</th>
      <th>Club</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @timekeepers.each do |timekeeper| %>
      <tr>
        <td><%= timekeeper.login %></td>
        <td><%= timekeeper.club.name %></td>
        <td><%= link_to 'Show', timekeeper %></td>
        <td><%= link_to 'Edit', edit_timekeeper_path(timekeeper) %></td>
        <td><%= link_to 'Destroy', timekeeper, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Timekeeper', new_timekeeper_path %>
