<%= form_for(@championship) do |f| %>
  <% if @championship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@championship.errors.count, "error") %> prohibited this championship from being saved:</h2>

      <ul>
      <% @championship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :season_id %><br />
    <%= f.number_field :season_id %>
  </div>
  <div class="field">
    <%= f.label :federation_id %><br />
    <%= f.number_field :federation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
